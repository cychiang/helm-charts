apiVersion: v1
kind: Service
metadata:
  name: {{ include "alb-ingress-routers.fullname" . }}
  labels:
    {{- include "alb-ingress-routers.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "alb-ingress-routers.selectorLabels" . | nindent 4 }}
